#**Задан кортеж (1, 2, 3, 4, 5, 4, 6, 2, 4, 7, 9, 4, 5, 6, 5, 0, 4, 2). Создать функцию 
#(в качестве параметров принимает исходный кортеж и символ, введенный пользователем), которая отображает кортеж, начинающийся 
#с первого вхождения и заканчивающийся последним вхождением данного символа.
#Если символа нет в кортеже - то необходимо вывести информацию об отсутствии данного элемента в кортеже.
#Если существует только один символ в кортеже - то отобразить кортеж, начинающийся с данного символа и заканчивающегося до конца исходного
#Пример:
#-        Если пользователь ввел число 4, тогда кортеж будет:
# (4, 5, 4, 6, 2, 4, 7, 9, 4, 5, 6, 5, 0, 4)
#-        Если пользователь ввел число 8, то необходимо отобразить сообщение:
#The tuple doesn't contain a character = 8
#-        Если пользователь ввел число 1, тогда кортеж будет:
#(9, 4, 5, 6, 5, 0, 4, 2)

def counter(tuple, a):

    if a not in tuple:
        print("The tuple doesn't contain a character = ", a)
    if a in tuple:
        ind = 0
        first_ind = float("inf")
        last_ind = 0
        for i in tuple:
            ind += 1
            if a == i:
                last_ind = ind
                if first_ind > ind:
                    first_ind = ind
        if first_ind == last_ind:
            print(tuple[first_ind - 1:])
        else:
            print(tuple[first_ind - 1:last_ind])
        print(first_ind, last_ind)


counter(tuple, a)
